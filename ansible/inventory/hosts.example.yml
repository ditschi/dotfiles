---
# Copy to hosts.yml and encrypt:
#   ./bootstrap  # or: machine inventory encrypt ansible/inventory/hosts.yml
# Vault password lives in Bitwarden (item name: ansible-vault).
#
# Do not put passwords in this example file.
all:
  children:
    debian:
      children:
        laptops:
          hosts:
            ditschi-ThinkPad-L13-Yoga-Gen-2:
              ansible_connection: local
        work: {}
        home: {}
        rpis:
          children:
            rpis_model_b: {}
            rpis_zero:
              hosts:
                rpi-zero-1:
                  ansible_host: 192.168.1.50
                  ansible_user: ditschi
        servers: {}
        desktop: {}
        headless: {}
