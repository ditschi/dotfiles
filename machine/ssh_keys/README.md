# Public keys for ansible role sshd_home (authorized_keys via ssh_allow_from).
# Filename = hostname entry in machine/hosts/*/ssh_allow_from (no path chars).
# Public keys only — never commit private keys.
